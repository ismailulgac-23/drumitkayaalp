version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: doctor_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: doctor_db
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - doctor_network

  adminer:
    image: adminer:latest
    container_name: doctor_adminer
    restart: unless-stopped
    ports:
      - "8081:8080"
    environment:
      ADMINER_DEFAULT_SERVER: postgres
    networks:
      - doctor_network
    depends_on:
      - postgres

volumes:
  postgres_data:
    driver: local

networks:
  doctor_network:
    driver: bridge

